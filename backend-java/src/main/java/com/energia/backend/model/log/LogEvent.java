package com.energia.backend.model.log;

import lombok.ToString;

@ToString
public enum LogEvent {

    LOGIN_ATTEMPT,
    LOGIN_SUCCESS,
    LOGIN_FAIL,

    USER_REGISTER,
    USER_APPROVED,
    USER_REJECTED,
    USER_EDITED,
    USER_ANONYMIZED,
    USER_ANONYMIZATION_REAPPLIED,

    ADMIN_ROLE_GRANTED,
    ADMIN_ROLE_REMOVED,

    BACKUP_RESTORE_RECONCILIATION,
    ANEEL_EXTRACTION_START,
    ANEEL_EXTRACTION_SUCCESS,
    ANEEL_EXTRACTION_FAIL,
    ANEEL_EXTRACTION_DUPLICATES_DETECTED,
    ADMIN_LOG_MODULE_ACCESS
}
